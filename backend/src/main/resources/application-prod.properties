# Nombre de la aplicación
spring.application.name=ecommerce-backend

# NOTA: En producción, se recomienda configurar:
# - server.port (si es diferente al puerto por defecto 8080)
# - spring.datasource.url, username y password
# - spring.jpa.hibernate.ddl-auto=none (no actualizar esquema automáticamente en producción)
# Estos valores pueden ser definidos a través de variables de entorno o un archivo de configuración seguro.
